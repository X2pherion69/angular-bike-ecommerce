import { BikeShopValidators } from './bike-shop-validators';

describe('BikeShopValidators', () => {
  it('should create an instance', () => {
    expect(new BikeShopValidators()).toBeTruthy();
  });
});
